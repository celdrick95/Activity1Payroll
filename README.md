# Activity1Payroll
A VB.NET payroll system that computes employee salary based on position example are (Admin, Personnel, Worker, or Utility). It calculates gross pay, tax deductions, and net pay, then displays the results on a second form.
